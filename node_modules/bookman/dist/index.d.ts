export interface BookmanOptions {
    defaultDir: string;
    databaseName: string;
    pretty?: boolean;
}
export declare const defaults: BookmanOptions;
export declare class Database {
    private options;
    private json;
    constructor(options?: BookmanOptions);
    private getData;
    private setData;
    private checkIfArray;
    private checkIfNumber;
    set<T>(key: string, value: T): T;
    push<T>(key: string, value: T): T[];
    pop<T>(key: string): T | undefined;
    shift<T>(key: string): T | undefined;
    unshift<T>(key: string, value: T): T[];
    add(key: string, value: number): number;
    subtract(key: string, value: number): number;
    delete(key: string): Promise<any>;
    getAll: () => any;
    fetchAll: () => any;
    map: () => any;
    all: () => any;
    get: <T>(key: string) => T;
    fetch: <T>(key: string) => T;
    has: (key: string) => boolean;
    destroy(): boolean;
}
